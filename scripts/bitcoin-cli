#/usr/bin/env zsh

if [[ ! -f src/bitcoin-cli ]]; then
    echo "wrapper warning: src/bitcoin-cli does not exist"
    exit 1
fi

echo "wrapper: running src/bitcoin-cli -datadir=$BITCOIN_DATADIR $@"
src/bitcoin-cli -datadir=$BITCOIN_DATADIR "$@"
