#/usr/bin/env zsh

if [[ ! -f src/bitcoind ]]; then
    echo "wrapper warning: src/bitcoind does not exist" >&2
    exit 1
fi

echo "wrapper: running src/bitcoind -datadir=$BITCOIN_DATADIR $@" >&2
src/bitcoind -datadir=$BITCOIN_DATADIR "$@"
